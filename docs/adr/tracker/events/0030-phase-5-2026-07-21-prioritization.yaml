adr_id: "0030"
phase_key: phase-5
phase_title: Phase 5 — GraphRAG
event: prioritization
date: "2026-07-21"
tracker_status: candidate
choices: >-
  Selected over 0026 Phase 4 (GPU bake-off — high score, deferred behind 0030);
  over 0032/0033 Phase 1 (Proposed — need Accept; intercalation option); over
  0031 (re-scope to .NET before Accept); over 0027 (Accept deferred). Single
  phase per pipeline rule; no ADR Accept required (0030 already Accepted).
  Invoker: continue 0030 P5; do not intercalate 0032; keep 0026 P4 deferred;
  0031 re-scope later Accept; 0032/0033 Accept later cycle. **Why now:** ADR
  0030 phases 1–4 merged; delivery order next is GraphRAG; `src/` has no Neo4j
  and Path D is Qdrant-only with explicit Phase-5 deferral; primary migration
  thread per prior open decisions; Proposed 0032/0033 need Accept if
  intercalated. Invoker confirmed continue 0030 Phase 5 this cycle.
  **Suggested scope:** one phase (= one PR). **Chosen scope:** Port
  `Neo4jStorage` + index-time graph writer; MCP `expand_search_context` gated by
  `GRAPH_ENABLED`; Neo4j Path D / callers when graph enabled; graph overlay
  Docker integration via `scripts/run_compose_integration.py`; document re-index
  after pull (no schema-version env); defer Phase 6 ColBERT/ops and Phase 7
  Python delete.
deviations: none
changelog:
  update: false
  reason: "user-facing unknown; invoker Changelog: no"
user_facing: false
open_decisions:
  - "RESOLVED — continue 0030 Phase 5 this cycle; do not intercalate 0032"
  - "RESOLVED — keep 0026 Phase 4 deferred behind 0030"
  - "RESOLVED — defer 0032/0033 Accept to later cycle"
  - "RESOLVED — ADR 0031 re-scope to .NET then Accept later (not this cycle)"
