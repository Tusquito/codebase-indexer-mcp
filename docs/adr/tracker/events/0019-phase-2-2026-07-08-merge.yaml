adr_id: 0019
phase_key: phase-2
phase_title: Phase 2 — Historical migration
event: merge
date: '2026-07-08'
tracker_status: merged
choices: 'squash merge `32b7279` on feature branch `adr/0019-phase-2-historical-migration` (squash of b61df4c, b7d94c6, f038e26, 63a9e58, 38f1e87); ADR accepted as **Accepted (phase 2)**; release skipped; Phase 3 (agent cutover) deferred'
deviations: none
code_evidence:
- 'merged via [PR #28](https://github.com/Tusquito/codebase-indexer-mcp/pull/28) (`adr/0019-phase-2-historical-migration`; squash `32b7279`)'
test_debt:
- carried from verification — Optional migrate-helper regression test skipped (helper is one-time/deletable per ADR)
- event-count text in tracker (141) is one behind on-disk (142) — cosmetic
verify: 'carried from verification — render `--check` zero drift; `--validate-only` 40 phase + 142 event files valid; 9 render unit tests pass; ruff clean; CI render-diff step confirmed blocking (no `|| true`); Docker integration report pass (quality validation skipped per plan); manual preamble + partial-acceptance/superseded postamble preserved outside markers; review rounds: 1'
git:
  pr_url: https://github.com/Tusquito/codebase-indexer-mcp/pull/28
  commit: 32b7279
  status: merged
changelog:
  update: false
  reason: user-facing no; release skipped
user_facing: false
