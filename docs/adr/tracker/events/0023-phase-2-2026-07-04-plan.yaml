adr_id: '0023'
phase_key: phase-2
phase_title: Phase 2 — Stop dual-write to Qdrant
event: plan
date: '2026-07-04'
tracker_status: planned
choices: 'Per-collection engine selection for mixed batches; Testcontainers Neo4j integration test in Phase 2; `[Unreleased]` CHANGELOG bullet in Phase 2; reuse Qdrant collection metadata key `graph_call_sites`; single PR per phase; pre-release: no `GRAPH_SCHEMA_VERSION` env; retain `callees` keyword index until Phase 3'
deviations: none
test_debt:
- Testcontainers Neo4j caller-query parity fixture
- mixed-collection per-engine routing
changelog:
  update: false
  reason: 'draft at verify; invoker Changelog: yes; user-facing yes'
user_facing: true
