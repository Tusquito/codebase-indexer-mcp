adr_id: "0033"
phase_key: phase-1
phase_title: Phase 1 — Core types + conventions
event: prioritization
date: "2026-07-22"
tracker_status: candidate
choices: >-
  Prefer 0033 P1 over 0034 P1 (~27), over Proposed 0027 (~24, needs Accept),
  over 0031 P2 / 0035 P2, over 0026 P4 (registry/GPU blocked); single phase
  per pipeline rule; no ADR Accept required (0033 already Accepted).
  **Why now:** ADR 0032 Phases 1–3 merged; Active tracker empty; prior
  prioritization deferred 0033 behind 0032; zero Result types in Domain while
  EmbeddingException/VectorStoreException and IndexJobNotFoundResponse remain;
  Accepted with clear Phase 1 validation; unlocks typed expected-failure path
  for Application/MCP without Accept work. **Suggested scope:** one phase
  (= one PR). **Chosen scope:** Add Domain `Result` / `Result<T>` / `Error` /
  `ErrorKind` (one type per file) + failure-vs-exception policy docs + unit
  tests for success/failure/`Match`; pick struct vs class in-phase; do not
  migrate Application ports or MCP tools (Phases 2–3); Docker integration
  required; defer 0034, 0031 P2, 0035 P2, 0026 P4, 0027.
deviations: none
changelog:
  update: false
  reason: "user-facing unknown; invoker Changelog: no"
user_facing: false
open_decisions:
  - "Proceed with 0033 Phase 1 this cycle?"
  - "Prefer 0034 Phase 1 instead?"
  - "NVIDIA GPU available for a later 0026 Phase 4?"
  - "Accept ADR 0027 in a future cycle?"
