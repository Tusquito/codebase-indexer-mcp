adr_id: "0032"
phase_key: phase-2
phase_title: Phase 2 — Indexing + Qdrant
event: prioritization
date: "2026-07-22"
tracker_status: candidate
choices: >-
  Selected over 0026 P4 (tie on score; lower scope/risk / no GPU session);
  over 0033 P1 (still Proposed; prior open decisions deferred behind 0032);
  over 0031 P2 / 0034 P1 / 0027; single phase per pipeline rule; no new ADR
  Accept required (0032 already Accepted). Human decisions applied: (1) no
  override to 0026 P4 — proceed 0032 P2; (2) accept 0033 and/or 0034 docs-only
  this cycle; (3) yes — CHANGELOG full re-index operator note for 0032 P2.
  **Why now:** 0030 P7 and 0032 P1 merged; active work empty; Qdrant still
  hard-codes named-vector string literals while Domain enums exist — finish
  Accepted Phase 2 before Proposed Result/TUnit or GPU bake-off.
  **Suggested scope:** one phase (= one PR). **Chosen scope:** Centralize
  `NamedVector` ↔ Qdrant `"dense"`/`"sparse"`/`"colbert"` via `DomainEnumWire`
  (or single helper) in `QdrantVectorStore` create/query/upsert/recommend
  paths; confirm chunker/classifier → payload enum round-trip; unit tests;
  Docker `scripts/run_compose_integration.py`; document re-index after pull
  (no schema-version env); defer Phase 3 search/xref/health enum surfaces.
deviations: none
changelog:
  update: false
  reason: "user-facing unknown; invoker Changelog: no"
user_facing: false
