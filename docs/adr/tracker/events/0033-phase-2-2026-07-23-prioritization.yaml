adr_id: "0033"
phase_key: phase-2
phase_title: Phase 2 — Application + index/job ports
event: prioritization
date: "2026-07-23"
tracker_status: candidate
choices: >-
  Prefer 0033 P2 (~29.5) over 0026 P4 (~28, GPU-blocked), over 0034 P1 (~25.5),
  over 0031 P2 / 0035 P2, over Proposed 0027 (needs Accept); single phase per
  pipeline rule; no ADR Accept required (0033 already Accepted).
  **Why now:** 0033 Phase 1 merged (PR #49); Domain Result types exist but
  Application still throws/`*ErrorResponse`; continuing the Accepted Result
  arc unlocks Phase 3 MCP envelope without Accept or GPU; 0026 P4 remains
  GPU-gated. **Suggested scope:** one phase (= one PR). **Chosen scope:**
  Migrate `IIndexJobService`, index pipeline / embed orchestration to
  `Result`/`Error`; replace job-conflict throws and untyped pipeline error
  bags; unit tests + Docker `scripts/run_compose_integration.py`; defer
  Phase 3 storage/embed ports + Host unified MCP error envelope; do not
  implement 0034/0026/0027 in this PR.
deviations: none
changelog:
  update: false
  reason: "user-facing unknown; invoker Changelog: no"
user_facing: false
open_decisions:
  - "Override to 0026 Phase 4 if NVIDIA GPU confirmed this cycle?"
  - "Prefer 0034 Phase 1 immediately after 0033 P2 vs finish 0033 P3 first?"
  - "Accept ADR 0027 in a future cycle?"
