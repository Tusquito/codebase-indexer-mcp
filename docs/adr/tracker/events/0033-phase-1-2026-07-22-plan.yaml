adr_id: "0033"
phase_key: phase-1
phase_title: Phase 1 — Core types + conventions
event: plan
date: "2026-07-22"
tracker_status: planned
choices: >-
  One PR = entire Phase 1; `readonly struct` Result + `sealed record` Error;
  instance `Match` on Result types (optional `ResultExtensions` only if needed);
  keep Domain PackageReference-free (no FluentResults/ErrorOr); leave
  `Exceptions/*` and `PipelineResult.Errors` strings intact; xUnit tests this
  cycle (defer 0034); Docker smoke without quality/perf; Accept after merge =
  no (already Accepted); final phase = no. Assumptions: ADR 0030 .NET Domain is
  production SoT; ADR 0033 already Accepted (no Accept work); pre-release allows
  introducing breaking APIs later without dual stacks; unused types do not
  change MCP JSON this phase; compose integration script remains the Docker
  gate; no config/env/re-index for Phase 1. Project phase policy: pre-release —
  no backward compatibility requirement unless ADR documents one; Docker
  integration always required; quality validation when required; no schema
  migration version env vars.
deviations: none
changelog:
  update: false
  reason: "invoker Changelog: no; status planned; user-facing no"
user_facing: false
open_decisions: []
